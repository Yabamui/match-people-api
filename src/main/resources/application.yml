server:
  port: 11000

spring:
  profiles:
    active: local
    include: custom-dev

  jpa:
    database: mysql
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.format_sql: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bomapp_b2b?autoReconnect=true&useUnicode=true&useSSL=false&characterEncoding=utf-8&serverTimezone=Asia/Seoul
    username: root
    password: qhaoq1234!@

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: debug